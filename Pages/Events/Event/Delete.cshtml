@page
@model LAE.Pages.Events.Sessions.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

@if (Model.LoggedInUser != null)
{
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Activity</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Activity.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Activity.Name)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Activity.TypeId)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Activity.TypeId)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Activity.MinGearScore)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Activity.MinGearScore)
            </dd>
        </dl>

        <form method="post">
            <input type="hidden" asp-for="Activity.Id" />
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a asp-page="./Index">Back to List</a>
        </form>
    </div>
}
else
{
    <h3>You are not logged in!</h3>
}